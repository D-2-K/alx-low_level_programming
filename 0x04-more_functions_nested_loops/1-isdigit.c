#include "main.h"

/**
 * _isdigit - checks for digits in a specified range
 * @c: input digit
 * Return: 1 if it is a digit on the specified range and 0 if otherwise
 */

int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0)
	}
}
