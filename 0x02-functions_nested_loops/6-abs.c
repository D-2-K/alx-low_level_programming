#include "main.h"

/**
 * _abs - computes absolute value of integer
 * @a: parameter
 * Return: absolute value of number
 */

int _abs(int a)
{
	int a;

	a = abs(5);
	_putchar("value of a = %d\n", a);

	return (0);
}
